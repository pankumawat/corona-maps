{
  "/health": {},
  "/help": {},
  "/login": {
    "method": "POST",
    "params": {
      "username": "string",
      "password": "string"
    },
    "description": "returns jwt token with 24 hour expiry"
  },
  "/details": {
    "method": "GET",
    "params": {
      "manager": "string"
    },
    "description": "Returns list of all the employees along with their geo location and risk score etc."
  },
  "/ll/:address": {
    "method": "GET",
    "params": {
      "address": "string"
    },
    "description": "Returns geo coordinates of the address."
  },
  "/master": {
    "method": "GET",
    "description": "Returns master data set with covid stats from all over India."
  }
}